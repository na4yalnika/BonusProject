<%= form_for(@transaction) do |f| %>
  <% if @transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transaction.errors.count, "error") %> prohibited this transaction from being saved:</h2>

      <ul>
      <% @transaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= f.text_field :recipient_id, :placeholder => "To", class: "form-control"  %>
  </div>
  <br>
  <div class="field">
    <%= f.text_field :tag, :placeholder => "Type", class: "form-control" %>
  </div>
  <br>
  <div class="field">
    <%= f.number_field :amount, :placeholder => "Amount", class: "form-control" %>
  </div>
  <br>
  <div class="field">
    <%= f.text_area :comment, :placeholder => "Comments", class: "form-control" %>
  </div>
  <br>
  
    <%= f.submit "Send", class: "btn btn-success" %>
    <%= link_to "Discard", root_path, class: "btn btn-danger" %>

<% end %>
